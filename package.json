{
  "name": "React Project",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify -t [ babelify --presets es2015 --presets react ] ./app/index.jsx -o main.js -v",
    "build": "browserify -t [ babelify --presets es2015 --presets react ] ./app/index.jsx -o main.js"
  },
  "license": "ISC",
  "dependencies": {
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "babel-core": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0"
  }
}
